# artisan command will create all migraton,model,seeder,store request,updateRequest,datatable,controller,route,tsx pages
 php artisan make:all products  --products is resource aname,..we can use college_students if our table is this
 
